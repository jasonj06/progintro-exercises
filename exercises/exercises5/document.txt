5.1 temperature
    float or byte depending on if you want decimals in the temperature

5.2 month
    byte (0-255) or string depending on if you want to represent a month by number or name

5.3 limits of integers
    1.
        sbyte (signed) -128 to 127 | 8 bit
        byte (unsigned) 0-255 | 8 bit
        short (signed) -32768 to 32768 | 16 bit
        ushort (unsigned) 0-65535 | 16 bit
        int (signed) | 32 bit
        int (unsigned 0-2^32 | 32 bit
        long (signed) | 64 bit
        long (unsigned) 0-2^64 | 64 bit

    2.
        byte

    3.
        5-3program/Project.cs

    4.
        it cycles to the lowest integer if 1 is added to its highest integer.

5.7 expression vs statement
    a statement is a step that ends with a semicolon and are executed one at a time in order (example int i = 1;). An expression is code that evaluates to a value (example i++;)

5.15 average age
    5-15program/Project.cs

    we create int variables for each person assigned with their corresponding age.
    we calculate the male age average, float explicit and write to console and assigns result to a float var.
    we calculate female age average, float explicit and write to console and assigns result to a float var.
    we calculate age average from the previous 2 variables.
    we calcuate age differemce from the previous 2 variables.

5.13 value vs variable
    the relationship between value and variable is that a value (data) is assigned to a variable (a container). once a value has been assigned to var, the value can be read or modified.

5.8 areas of a circle
    5-8program/Project.cs